import { NsInputDirective } from './ns-input.directive';

describe('NsInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NsInputDirective();
    expect(directive).toBeTruthy();
  });
});
